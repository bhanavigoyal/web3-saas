export default function Page(){
    // map all the tasks by the wallet here and if the user does not has any task return no tasks
    // give the option to click on any task and see that
    return <div>
        all tasks
        
    </div>
}